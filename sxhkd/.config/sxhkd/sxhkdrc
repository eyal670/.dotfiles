# terminal emulator
super + shift + Return 
  alacritty -e tmux a

# rofi
ctrl + grave ; {grave,1} 
  rofi {-modi "clipboard:greenclip print" -show clipboard -run-command '\{cmd\}',-show drun -show-icons}

# media keys #
#play/pause spt
super + shift + p 
  spt playback -t 

# list keybinding
super + shift + question
  sh $HOME/Scripts/hotkeys.sh 

# make sxhkd reload its configuration files:
super + shift + Escape
	pkill -USR1 -x sxhkd
